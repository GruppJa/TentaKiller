<Page x:Class="TentaKiller.Views.ChallangePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="ChallangePage">

    <StackPanel>
        <TextBlock HorizontalAlignment="Center" Text="{Binding Id}"></TextBlock>

        <TextBlock HorizontalAlignment="Center" FontSize="14">Question</TextBlock>
        <TextBlock Text="{Binding Question}"></TextBlock>

        <TextBlock HorizontalAlignment="Center" FontSize="14">Answer</TextBlock>
        <TextBlock Text="{Binding Answer}"></TextBlock>

        <TextBlock HorizontalAlignment="Center" FontSize="14">Lies</TextBlock>
        <ListView ItemsSource="{Binding Lies}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}"></TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
